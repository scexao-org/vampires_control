server: 127.0.0.1:8792
procs:
  device_server:
    shell: "python -i /home/lestat/src/device_control/src/device_control/daemons/vampires_devices.py"
  qwp_tracking: "sleep 5; vampires_qwp_daemon"
  temp_status: "sleep 5; vampires_temp_daemon"
  healthcheck:
    shell: "healthcheck"
    autostart: false
  set_datatype:
    shell: "set_datatype"
    autostart: false
  preplog:
    shell: "startlog"
    autostart: false
  startlog:
    shell: "resumelog"
    autostart: false
  stoplog:
    shell: "pauselog"
    autostart: false
  stoplog(wait):
    shell: "pauselog --wait"
    autostart: false
  killlog:
    shell: "stoplog --kill"
    autostart: false
  autofocus: 
    shell: "autofocus all"
    autostart: false
  prepare_nominal:
    shell: "prep_nominal"
    autostart: false
  prepare_pdi: 
    shell: "prep_pdi"
    autostart: false
  prepare_sdi: 
    shell: "prep_sdi"
    autostart: false
  badseeing:
    shell: "while true; do sl; done"
    autostart: false